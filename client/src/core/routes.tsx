import { Routes } from "../types/routes";

export const routes: Routes = {
  home: "/",
  auth: "/auth",
  test: "/test",
  para: "/para",
  codeLab: "/codelab",
  conceptFactory: "/conceptFactory",
};
